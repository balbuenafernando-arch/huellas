"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { Edit, Eye, Plus, Trash2, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { FriendlyError, PageSkeleton } from "@/components/feedback";
import { listMyCases, type CaseRecord } from "@/lib/cases";
import { deleteRegisteredPet, listMyRegisteredPets, type RegisteredPet } from "@/lib/sprint14-store";
import { friendlyError, operationError } from "@/lib/form-validation";
export default function MisMascotasPage(){const[pets,setPets]=useState<RegisteredPet[]>([]),[cases,setCases]=useState<CaseRecord[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[toast,setToast]=useState(""),[pendingDelete,setPendingDelete]=useState<RegisteredPet|null>(null),[deleting,setDeleting]=useState(false);async function load(){try{const[nextPets,nextCases]=await Promise.all([listMyRegisteredPets(),listMyCases()]);setPets(nextPets);setCases(nextCases);setError("");}catch(caught){setError(friendlyError(caught,"No se pudieron cargar tus mascotas."));}finally{setLoading(false);}}useEffect(()=>{void load();const params=new URLSearchParams(window.location.search);if(params.get("registrada")==="1"||params.get("actualizada")==="1"){setToast(params.get("actualizada")==="1"?"Mascota actualizada correctamente.":"Mascota registrada correctamente.");window.history.replaceState(null,"","/mis-mascotas");}},[]);useEffect(()=>{if(!toast)return;const timer=setTimeout(()=>setToast(""),4000);return()=>clearTimeout(timer);},[toast]);useEffect(()=>{if(!pendingDelete)return;const previous=document.body.style.overflow;document.body.style.overflow="hidden";return()=>{document.body.style.overflow=previous;};},[pendingDelete]);async function remove(){if(!pendingDelete||deleting)return;setDeleting(true);try{await deleteRegisteredPet(pendingDelete.id);setPendingDelete(null);await load();setToast("Mascota eliminada correctamente.");}catch(caught){setError(operationError(caught,"eliminar mascota"));}finally{setDeleting(false);}}if(loading)return <PageSkeleton/>;return <main className="container py-6">{toast&&<div className="fixed right-4 top-20 z-[1300] flex max-w-sm items-center gap-3 rounded-xl bg-[#E1F5EE] p-4 text-sm font-semibold text-[#085041] shadow-xl" role="status"><span>{toast}</span><button onClick={()=>setToast("")} aria-label="Cerrar mensaje"><X size={16}/></button></div>}{pendingDelete&&<div className="fixed inset-0 z-[1100] grid place-items-center overflow-hidden bg-black/60 p-4" role="dialog" aria-modal="true"><section className="form-card max-h-[calc(100dvh-24px)] max-w-lg space-y-4 overflow-y-auto"><h2 className="text-xl font-bold">Eliminar mascota</h2><p>¿Estás seguro de que deseas eliminar a {pendingDelete.nombre}? Se eliminarán su ficha y fotografías. Esta acción no puede deshacerse.</p><div className="grid gap-2 min-[390px]:grid-cols-2"><Button variant="outline" onClick={()=>setPendingDelete(null)} disabled={deleting}>Cancelar</Button><Button onClick={remove} disabled={deleting}><Trash2 size={18}/>{deleting?"Eliminando...":"Eliminar definitivamente"}</Button></div></section></div>}<div className="mb-5 flex flex-col gap-3 min-[390px]:flex-row min-[390px]:items-end min-[390px]:justify-between"><div><h1 className="font-serif text-4xl">Mis mascotas</h1><p className="mt-2 text-[#6B6860]">Gestiona las fichas preventivas de tus mascotas.</p></div><Button asChild><Link href="/mis-mascotas/nueva"><Plus size={18}/>Registrar mascota</Link></Button></div>{error&&<div className="mb-4"><FriendlyError message={error} onRetry={load}/></div>}{!pets.length?<div className="form-card empty-state"><span className="text-4xl">🐾</span><strong>Aún no registraste mascotas.</strong><span>Crea una ficha preventiva con sus datos y fotografías.</span><Button asChild><Link href="/mis-mascotas/nueva">Registrar mi primera mascota</Link></Button></div>:<section className="grid gap-3 md:grid-cols-2">{pets.map((pet)=>{const active=cases.find((item)=>item.petId===pet.id&&item.status!=="reunido"&&item.status!=="archivado");return <article key={pet.id} className="form-card"><img src={pet.foto_principal??pet.foto_url} alt={pet.nombre} className="h-44 w-full rounded-xl bg-[#F8F7F4] object-contain"/><div className="mt-3 flex flex-wrap items-center gap-2"><h2 className="font-bold">{pet.nombre}</h2><span className={`status-pill ${active?"status-perdido":"status-reunido"}`}>{active?"Búsqueda activa":"Sin búsqueda activa"}</span></div><p className="text-sm text-[#6B6860]">{pet.especie} · {pet.raza}</p><div className="mt-3 grid gap-2 min-[390px]:grid-cols-2"><Button size="sm" variant="outline" asChild><Link href={`/mis-mascotas/${pet.id}`}><Eye size={16}/>Ver detalle</Link></Button><Button size="sm" variant="outline" asChild><Link href={`/mis-mascotas/${pet.id}/editar`}><Edit size={16}/>Editar</Link></Button>{active?<Button size="sm" asChild><Link href={`/pet/${active.id}`}>Ver búsqueda</Link></Button>:<Button size="sm" asChild><Link href={`/perdi-mi-mascota?petId=${pet.id}`}>Reportar pérdida</Link></Button>}<Button size="sm" variant="outline" onClick={()=>setPendingDelete(pet)}><Trash2 size={16}/>Eliminar</Button></div></article>;})}</section>}</main>;}
